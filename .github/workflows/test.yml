on:
  push:
    branches:
    - 'delete_*'
  pull_request:
    branches:
    - master

jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run simple command
        run: echo "Hello world"
